public class outputt {
    public static void main(String[] args) {
        int[] arr= { 3 , 5,3, 5, 6, 2,7}; 
        System.out.println(arr.length);
        for(int i=0; i<arr.length; i++ ){
            System.out.print(arr[i] + " ");


        }
    }
    
}           // This code defines a class named `outputt` with a `main` method. Inside the `main` method, 
            // an integer array `arr` is initialized with specific values. The length of the array is printed to the console, 
            // followed by a loop that iterates through each element of the array and prints it, separated by a space.
